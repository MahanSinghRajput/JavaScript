//fetch is a global method for fetching a resource from the network.
//fetch uses a special queue to reach the call stack, that's why it's response is faster.
//see fetchStructure.png for internal structure